import 'package:flutter/material.dart';

class AppLocalization {
  static List<Locale> localList = [tr, en];
  static const Locale tr = Locale("tr", "TR");
  static const Locale en = Locale("en", "US");
  static const String path = 'asset/lang';
}
